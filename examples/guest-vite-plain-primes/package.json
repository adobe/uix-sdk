{
  "name": "guest-vite-plain-primes",
  "description": "Primes Guy",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "clean": "rm -rf dist",
    "example:build": "vite build",
    "example:dev": "vite",
    "example:demo": "vite preview --mode production"
  },
  "dependencies": {
    "@adobe/uix-guest": "*"
  }
}
