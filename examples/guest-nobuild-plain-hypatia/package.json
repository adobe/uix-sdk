{
  "name": "guest-nobuild-plain-hypatia",
  "description": "Hypatia",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "example:build": "echo ''",
    "example:development": "rm -rf ./dist && mkdir -p dist && cp ../../packages/uix-guest/dist/index.global.* ./dist/ && serve -p $MULTI_SERVER_PORT .",
    "example:production": "npm run -s example:development"
  },
  "dependencies": {
    "@adobe/uix-guest": "*"
  }
}
