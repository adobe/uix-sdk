{
  "name": "guest-nobuild-plain-hypatia",
  "description": "Hypatia",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "example:build": "echo ''",
    "example:development": "rm -rf ./dist; ln -s ../../packages/uix-guest/dist/ ./dist && serve -p $MULTI_SERVER_PORT .",
    "example:production": "npm run -s example:development"
  },
  "dependencies": {
    "@adobe/uix-guest": "*"
  }
}
