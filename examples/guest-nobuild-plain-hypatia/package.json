{
  "name": "guest-nobuild-plain-hypatia",
  "private": "true",
  "version": "0.0.1",
  "description": "Hypatia",
  "scripts": {
    "clean": "del ./dist",
    "example:build": "echo ''",
    "example:development": "serve -p $MULTI_SERVER_PORT .",
    "example:production": "npm run -s example:development",
    "preexample:development": "del ./dist; ln -s ../../packages/uix-guest/dist/ ./dist"
  },
  "dependencies": {
    "@adobe/uix-guest": "*"
  }
}
