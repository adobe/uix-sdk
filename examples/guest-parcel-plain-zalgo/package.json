{
  "name": "guest-parcel-plain-zalgo",
  "description": "Zalgo",
  "private": true,
  "version": "0.0.1",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "clean": "rm -rf dist",
    "example:build": "parcel build src/index.html",
    "example:development": "parcel --log-level warn -p $MULTI_SERVER_PORT src/index.html",
    "example:production": "serve -p $MULTI_SERVER_PORT dist"
  },
  "dependencies": {
    "@adobe/uix-guest": "*",
    "humanize-duration": "^3.27.2",
    "zalgo-js": "^1.0.0"
  }
}
