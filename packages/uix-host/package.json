{
  "name": "@adobe/uix-host",
  "version": "0.5.3",
  "description": "Tools for embedding UIX Extensions into Adobe apps",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "README.md",
    "tsconfig.json"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "run-p compile:*",
    "clean": "rm -rf dist *.tsbuildinfo docs/temp",
    "compile:bundle": "tsup",
    "compile:emit": "tsc --build tsconfig.json",
    "debug-build": "tsc --project tsconfig-debug.json --verbose",
    "watch": "run-p watch:*",
    "watch:bundle": "tsup --watch --silent",
    "watch:emit": "tsc --watch"
  },
  "browserslist": [
    "> 0.2%, last 2 versions, not dead"
  ],
  "repository": {
    "type": "git",
    "url": "https://git.corp.adobe.com/dx-devex-acceleration/uix-sdk.git"
  },
  "author": "Adobe, Inc,",
  "license": "UNLICENSED",
  "dependencies": {
    "@adobe/uix-core": "^0.5.3",
    "penpal": "~6.2.2"
  }
}