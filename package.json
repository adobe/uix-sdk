{
  "name": "@adobe/uix-sdk",
  "version": "0.0.2",
  "description": "Tools for embedding UI Extensions into Adobe apps",
  "publishConfig": {
    "registry": "https://artifactory.corp.adobe.com/artifactory/api/npm/npm-adobe-release/"
  },
  "type": "module",
  "browser": "dist/index.js",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./*": "./dist/*/index.js"
  },
  "files": [
    "dist",
    "src",
    "tsconfig.json"
  ],
  "scripts": {
    "build": "concurrently 'npm:clean' 'npm:format' && npm run -s compile:prod",
    "clean": "rm -rf dist && rm -rf examples/*/dist && rm -rf *.tsbuildinfo",
    "dev": "npm run -s compile && npm run -s dev:link && node scripts/dev-server.js",
    "dev:link": "yalc publish --quiet --push --update",
    "compile": "tsc --build",
    "compile:debug": "tsc --build tsconfig-debug.json --verbose",
    "compile:watch": "tsc-watch --noClear --onSuccess 'npm run -s dev:link'",
    "compile:prod": "tsc --project tsconfig-prod.json",
    "format": "prettier --write src",
    "lint": "prettier --check src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "browserslist": [
    "> 0.2%, last 2 versions, not dead"
  ],
  "repository": {
    "type": "git",
    "url": "https://git.corp.adobe.com/dx-devex-acceleration/uix-sdk.git"
  },
  "author": "Adobe, Inc,",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/react": "^17.0.16",
    "@types/use-sync-external-store": "^0.0.3",
    "concurrently": "^7.2.2",
    "penpal": "^6.2.2",
    "prettier": "^2.6.2",
    "react": "^17.0.2",
    "ts-loader": "^9.3.0",
    "tsc-watch": "^5.0.3",
    "typescript": "^4.7.2",
    "yalc": "^1.0.0-pre.53"
  },
  "peerDependencies": {
    "penpal": "^6.2.2",
    "react": "^17.0.2"
  },
  "dependencies": {
    "use-sync-external-store": "^1.2.0"
  }
}
